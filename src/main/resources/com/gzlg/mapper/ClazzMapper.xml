<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzlg.mapper.ClazzMapper">

    <!-- 条件分页查询班级列表（关联班主任姓名） -->
    <select id="list" resultType="com.gzlg.pojo.Clazz">
        select c.id, c.name, c.room, c.begin_date, c.end_date, c.master_id,
               c.subject, c.create_time, c.update_time, e.name masterName
        from clazz c
        left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                and c.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and c.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>

    <!-- 根据ID删除班级 -->
    <delete id="deleteById">
        delete from clazz where id = #{id}
    </delete>

    <!-- 根据ID查询班级 -->
    <select id="selectById" resultType="com.gzlg.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, subject,
               create_time, update_time
        from clazz
        where id = #{id}
    </select>

    <!-- 根据ID更新班级 -->
    <update id="updateById">
        update clazz
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="room != null">room = #{room},</if>
            <if test="beginDate != null">begin_date = #{beginDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="masterId != null">master_id = #{masterId},</if>
            <if test="subject != null">subject = #{subject},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 查询所有班级 -->
    <select id="listAll" resultType="com.gzlg.pojo.Clazz">
        select id, name, room, begin_date, end_date, master_id, subject,
               create_time, update_time
        from clazz
        order by update_time desc
    </select>

    <!-- 根据班级ID查询关联的学生数量 -->
    <select id="countStudentByClazzId" resultType="java.lang.Integer">
        select count(*) from student where clazz_id = #{clazzId}
    </select>

</mapper>
