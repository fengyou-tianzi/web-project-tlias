<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzlg.mapper.EmpMapper">
    <select id="list" resultType="com.gzlg.pojo.Emp">
        select e.id, e.username, e.name, e.gender,e.image,e.job, e.salary ,e.entry_date, e.dept_id, d.name
        deptName,e.create_time, e.update_time
        from emp e
        left join dept d on e.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                and e.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and e.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and e.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by e.update_time desc
    </select>
</mapper>